The module wrapper for violence detection system, include ultilities, initialization,  run(), val(), stream().

run() -> the testing method: using for debug and understand system behavior
val() -> Simplifies version of model, used for testing dataset
stream() -> inference of the system, carefully design for UI/UX optimization
